/**
 * Переключатели того, что показывать клиенту.
 *
 * LOYALTY_VISIBLE: программа лояльности пока не согласована, поэтому баллы,
 * уровень, вкладка «Карта» и реферальная программа скрыты. Начисление на сервере
 * продолжает идти и история сохраняется — когда условия утвердят, достаточно
 * поставить true и выпустить сборку, ничего не пересчитывая.
 * Тот же флаг есть в вебе: apps/api/app/cabinet/page.tsx.
 */
export const LOYALTY_VISIBLE = false;
